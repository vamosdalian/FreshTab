{
  "name": "freshtab",
  "version": "1.0.0",
  "description": "一个美观且功能丰富的 Chrome 新标签页替换扩展。",
  "main": "newtab.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:extension": "vite build && npm run copy:extension",
    "copy:extension": "cp dist/index.html dist/newtab.html && sed -i '' 's|\"/assets/|\"./assets/|g' dist/newtab.html && cp manifest.json dist/ && cp -r icons dist/",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "vite": "^6.3.5",
    "vue": "^3.5.16"
  }
}
